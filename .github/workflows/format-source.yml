name: Format Powerline Source

on:
  push:
    branches:
      - agent/normalize-powerline-source

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/normalize-powerline-source
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Install dependencies
        working-directory: flutter_app
        run: flutter pub get
      - name: Format every tracked Dart file
        working-directory: flutter_app
        run: dart format lib test integration_test
      - name: Commit all formatter output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u
          git diff --cached --quiet || git commit -m "Finish formatting Dart source"
          git push origin HEAD:agent/normalize-powerline-source
