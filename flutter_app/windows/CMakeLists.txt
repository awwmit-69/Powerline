cmake_minimum_required(VERSION 3.14)
project(powerline LANGUAGES CXX)
set(BINARY_NAME "powerline")
# NOTE: Minimal reference. Run `flutter create .` on a Windows host with the
# Flutter SDK + Visual Studio toolchain to regenerate the full runner.
