cmake_minimum_required(VERSION 3.13)
project(runner LANGUAGES CXX)
set(BINARY_NAME "powerline")
set(APPLICATION_ID "com.azdglobal.powerline")
cmake_policy(SET CMP0063 NEW)
# NOTE: This is a minimal reference. Run `flutter create .` on a Linux host
# with the Flutter SDK to generate the full desktop runner + ephemeral files.
